import "DataServices.pkl"
import "JWT.pkl"
import "StorageServices.pkl"

db = new DataServices.DbProperties {
  mongo = new DataServices.DbConnectionProperties {
    host = "localhost"
    port = 27017

    auth = new DataServices.DbConnectionAuthProperties {
      username = "root"
      password = "root"
    }
  }

  milvus = new DataServices.DbConnectionProperties {
    host = "localhost"
    port = 19530

    auth = new DataServices.DbConnectionAuthProperties {
      username = ""
      password = ""
    }
  }

  redis = new DataServices.DbConnectionProperties {
    host = "localhost"
    port = 6379

    auth = new DataServices.DbConnectionAuthProperties {
      username = ""
      password = "root"
    }
  }
}


tokens = new JWT.Tokens {
  accessToken = JWT.tokenPropertiesFactory.create("access_token_secret", 2.min)
  refreshToken = JWT.tokenPropertiesFactory.create("refresh_token_secret", 120.d)
}

storage = new StorageServices.StorageServices {
  s3 = new StorageServices.S3ConnectionProperties {
    bucketName = "testing-bucket"
    region = "us-east-1"
    endpoint = "http://localhost:4566"
    credentials = new StorageServices.S3ConnectionCredentials {
      accessKeyId = "access-key-id"
      secretAccessKey = "access-key-secret"
    }
  }
}